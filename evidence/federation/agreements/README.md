# Federation fixture agreements

Machine-readable peer records with `federationLevel: fixture`.

| File | Peers | Level | Human agreement |
| --- | --- | --- | --- |
| `fixture-lean-smt.json` | lean-smt → MathEvidence | fixture | OPEN |
| `fixture-cslib.json` | MathEvidence → cslib | fixture | OPEN |

Schema: `schemas/federation-agreement.schema.json`.

These records do **not** close live federation (ME-404). Live agreements remain
in `docs/architecture/federation-agreements.md` and stay OPEN until real
maintainer consent exists.
